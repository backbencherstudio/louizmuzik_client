import { Inter } from "next/font/google"

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
})

export const interBlackItalic = Inter({
  subsets: ["latin"],
  weight: "900",
  style: "italic",
  display: "swap",
})

